name: Deploy to Staging

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Cloud Run (Staging)
        run: echo "TODO: Add GCP Cloud Run deploy step after GCP setup"
